# 剪贴板助手 - 架构文档

## 1. 项目概述

剪贴板助手是一款基于 PyQt5 开发的 Windows 桌面应用程序，提供智能剪贴板管理功能。支持自动捕获复制内容、批量粘贴、多种输出模式以及主题定制。

**核心特性:**
- Ctrl+C 自动捕获剪贴板内容
- 批量粘贴（逗号拼接/顺序输出/倒序输出）
- 系统托盘常驻
- 日间/暗夜主题切换
- 多配色方案支持
- 全局快捷键操作

---

## 2. 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.x | 主要开发语言 |
| PyQt5 | >=5.15.0 | GUI 框架 |
| pywin32 | >=311 | Windows API 交互 |
| pynput | >=1.7.6 | 全局快捷键监听 |
| pyperclip | >=1.8.2 | 剪贴板备用方案 |
| PyInstaller | >=6.0.0 | 应用打包 |

---

## 3. 项目结构

```
clipboard-app/
├── main.py                          # 主入口文件
├── requirements.txt                 # 依赖配置
├── BUILD_GUIDE.txt                  # 打包指南
├── data/                            # 数据存储目录
│   ├── clipboard_data.json          # 剪贴板数据
│   └── settings.json                # 用户设置
├── themes/                          # 主题模块
│   ├── __init__.py
│   └── theme_manager.py             # 主题管理器
├── ui/                              # UI 组件模块
│   ├── __init__.py
│   └── components/                  # UI 子组件
│       ├── __init__.py
│       ├── clickable_label.py       # 可点击标签组件
│       └── gradient_header.py       # 渐变头部组件（已集成到 main.py）
└── utils/                           # 工具模块
    ├── __init__.py
    ├── clipboard_utils.py           # 剪贴板工具
    ├── keyboard_simulator.py        # 键盘模拟
    ├── window_manager.py            # 窗口管理
    └── data_manager.py              # 数据持久化
```

---

## 4. 架构设计

### 4.1 总体架构

```
┌─────────────────────────────────────────────────────────┐
│                    主应用层 (main.py)                     │
│  ┌─────────────────┐         ┌────────────────────┐     │
│  │ ClipboardWindow │◄────────┤ SettingsDialog     │     │
│  │  (主窗口)        │         │  (设置对话框)       │     │
│  └────────┬────────┘         └────────────────────┘     │
│           │                                              │
│           │ 依赖                                         │
└───────────┼──────────────────────────────────────────────┘
            │
     ┌──────┴──────┬───────────┬──────────┬────────────┐
     │             │           │          │            │
┌────▼─────┐  ┌───▼───┐  ┌───▼───┐  ┌──▼──┐  ┌──────▼─────┐
│ Themes   │  │  UI   │  │ Utils │  │ Data│  │  System    │
│ 主题系统  │  │ 组件  │  │ 工具  │  │ 数据│  │  API       │
└──────────┘  └───────┘  └───────┘  └─────┘  └────────────┘
```

### 4.2 模块职责

#### 4.2.1 主应用模块 (main.py)

**核心类:**

1. **ClipboardWindow** - 主窗口类
   - 位置: `main.py:380-1223`
   - 职责: 应用主界面，剪贴板管理核心逻辑
   - 关键属性:
     - `clipboard_data`: 剪贴板数据列表
     - `theme_manager`: 主题管理器实例
     - `data_manager`: 数据管理器实例
     - `sequential_index`: 顺序/倒序输出索引
   - 核心功能:
     - 剪贴板监听与自动捕获 (`check_clipboard_change`)
     - 批量粘贴操作 (`do_paste`)
     - 单项粘贴 (`paste_single_item`)
     - UI 更新与渲染 (`update_list`)
     - 全局快捷键注册 (`register_hotkeys`)

2. **SettingsDialog** - 设置对话框类
   - 位置: `main.py:41-378`
   - 职责: 用户设置管理界面
   - 支持配置:
     - 输出模式: 逗号拼接/顺序输出/倒序输出
     - 系统主题: 日间/暗夜模式
     - 配色方案: 蓝渐变/纯蓝色/纯绿色

**关键函数:**

- `check_single_instance()` (1225-1253): 单实例检测，使用 Windows 互斥量
- `main()` (1256-1276): 应用入口点

---

#### 4.2.2 主题模块 (themes/)

**ThemeManager 类** (`theme_manager.py:7-467`)

职责: 统一管理应用主题和样式

**配色方案定义:**
```python
COLOR_SCHEMES = {
    'blue_gradient': {
        'light': ('#667eea', '#764ba2'),
        'dark': ('#4a5568', '#2d3748')
    },
    'pure_blue': {
        'light': ('#7ba3d6', '#7ba3d6'),
        'dark': ('#5a7a9c', '#5a7a9c')
    },
    'pure_green': {
        'light': ('#7fb896', '#7fb896'),
        'dark': ('#5a8c6e', '#5a8c6e')
    }
}
```

**核心方法:**
- `get_colors()`: 获取当前配色方案的颜色
- `get_label_style()`: 标签样式
- `get_list_item_style()`: 列表项样式
- `get_primary_button_style()`: 主按钮样式
- `get_combo_box_style()`: 下拉框样式
- `get_container_style()`: 容器样式

---

#### 4.2.3 UI 组件模块 (ui/components/)

**ClickableLabel 类** (`clickable_label.py:9-49`)

职责: 可点击的标签组件，用于列表项点击粘贴

关键特性:
- 继承自 QLabel
- 支持自定义点击回调
- 自动设置鼠标指针为手型
- 数据绑定机制

使用示例:
```python
label = ClickableLabel(
    text="复制的文本",
    callback=self.paste_single_item,
    data="数据内容"
)
```

---

#### 4.2.4 工具模块 (utils/)

##### 1. clipboard_utils.py - 剪贴板工具

**核心函数:**

- `get_clipboard_text()` (11-36)
  - 功能: 安全获取剪贴板文本
  - 特性: 支持重试机制（3次，50ms 延迟）
  - 返回: 文本内容或 None

- `set_clipboard_text(text)` (39-66)
  - 功能: 安全设置剪贴板文本
  - 特性: 支持重试机制
  - 返回: 成功返回 True，失败返回 False

- `process_clipboard_text(text)` (69-86)
  - 功能: 处理剪贴板文本，分割多行
  - 返回: 文本列表

##### 2. keyboard_simulator.py - 键盘模拟

**核心结构体:**
- `KEYBDINPUT`: 键盘输入结构
- `INPUT`: Windows SendInput API 输入结构

**核心函数:**

- `send_input_key(vk, up=False)` (71-90)
  - 功能: 使用 SendInput 发送按键事件
  - 参数: vk (虚拟键码), up (是否释放按键)

- `simulate_paste()` (93-118)
  - 功能: 模拟 Ctrl+V 粘贴操作
  - 流程:
    1. 释放所有修饰键 (Ctrl/Alt/Shift)
    2. 模拟 Ctrl 按下
    3. 模拟 V 键按下与释放
    4. 释放 Ctrl

- `simulate_key_combo(*keys)` (121-147)
  - 功能: 通用组合键模拟

##### 3. window_manager.py - 窗口管理

**核心函数:**

- `get_foreground_window()` (10-23)
  - 功能: 获取当前活动窗口句柄
  - 返回: 窗口句柄或 None

- `set_foreground_window(hwnd)` (26-45)
  - 功能: 激活指定窗口
  - 参数: hwnd (窗口句柄)

**WindowFocusManager 类** (48-94)
- 职责: 窗口焦点管理
- 方法:
  - `save_current_window()`: 保存当前窗口
  - `restore_previous_window()`: 恢复之前窗口
  - `get_previous_window()`: 获取之前窗口句柄

##### 4. data_manager.py - 数据持久化

**DataManager 类** (10-62)

基础数据管理器，提供 JSON 文件读写

- `load_json(file_path, default=None)`: 加载 JSON
- `save_json(file_path, data, indent=2)`: 保存 JSON

**ClipboardDataManager 类** (64-154)

继承自 DataManager，专门管理剪贴板数据

核心方法:
- `load_clipboard_data()`: 加载剪贴板数据，自动标准化格式
- `save_clipboard_data(data)`: 保存剪贴板数据
- `load_settings(default_settings)`: 加载设置，支持默认值合并
- `save_settings(settings)`: 保存设置

---

## 5. 核心流程

### 5.1 应用启动流程

```
main()
  │
  ├─► check_single_instance()         # 检测单实例
  │    └─► 使用 Windows 互斥量
  │
  ├─► QApplication 初始化
  │
  ├─► ClipboardWindow 实例化
  │    ├─► load_settings()            # 加载用户设置
  │    ├─► init_ui()                  # 初始化界面
  │    ├─► load_data()                # 加载剪贴板数据
  │    ├─► register_clipboard_listener()  # 注册剪贴板监听
  │    ├─► register_hotkeys()         # 注册全局快捷键
  │    ├─► init_tray_icon()           # 初始化系统托盘
  │    └─► apply_theme()              # 应用主题
  │
  ├─► window.show()                   # 显示主窗口
  │
  └─► app.exec_()                     # 进入事件循环
```

### 5.2 剪贴板监听流程

```
clipboard_check_timer (300ms 轮询)
  │
  └─► check_clipboard_change()
       │
       ├─► 检查是否在忽略时间窗口内
       │
       ├─► get_clipboard_text()        # 获取剪贴板内容
       │
       ├─► 对比 last_clipboard_text    # 检测变化
       │
       ├─► 检查是否是自己刚粘贴的内容
       │
       └─► clipboard_changed_signal.emit()
            │
            └─► on_clipboard_changed()
                 │
                 ├─► process_clipboard_text()  # 处理文本（分割多行）
                 │
                 ├─► 添加到 clipboard_data    # 去重添加
                 │
                 ├─► update_list()            # 更新界面
                 │
                 ├─► save_data()              # 持久化
                 │
                 └─► show_notification()      # 显示通知（窗口隐藏时）
```

### 5.3 批量粘贴流程（Ctrl+Space）

```
Ctrl+Space 快捷键
  │
  └─► do_paste_signal.emit()
       │
       └─► do_paste()
            │
            ├─► 判断输出模式
            │
            ├─► 【逗号拼接模式】
            │    ├─► 拼接所有文本（逗号分隔）
            │    ├─► set_clipboard_text(combined_text)
            │    └─► QTimer.singleShot(250ms, _execute_paste)
            │
            ├─► 【顺序输出模式】（FIFO）
            │    ├─► 初始化索引到第一项（如需要）
            │    ├─► 获取 clipboard_data[sequential_index]
            │    ├─► set_clipboard_text(text)
            │    ├─► QTimer.singleShot(250ms, _execute_paste)
            │    └─► sequential_index += 1（循环递增）
            │
            └─► 【倒序输出模式】（LIFO）
                 ├─► 初始化索引到最后一项（如需要）
                 ├─► 获取 clipboard_data[sequential_index]
                 ├─► set_clipboard_text(text)
                 ├─► QTimer.singleShot(250ms, _execute_paste)
                 └─► sequential_index -= 1（循环递减）
```

### 5.4 单项粘贴流程

```
点击列表项 (ClickableLabel)
  │
  └─► paste_single_item(text)
       │
       ├─► 记录粘贴内容和时间戳
       │    ├─► last_pasted_text = text
       │    ├─► last_paste_time = time.time()
       │    └─► paste_ignore_until = time.time() + 1.5
       │
       ├─► set_clipboard_text(text)
       │
       ├─► hide()                        # 隐藏窗口
       │
       ├─► set_foreground_window(previous_window)  # 激活前一个窗口
       │
       ├─► time.sleep(0.1)               # 等待窗口激活
       │
       └─► QTimer.singleShot(400ms, _execute_single_paste)
            │
            └─► simulate_paste()          # 模拟 Ctrl+V
```

---

## 6. 数据模型

### 6.1 剪贴板数据格式

```json
[
  {
    "text": "复制的文本内容",
    "timestamp": "14:30:25"
  },
  {
    "text": "另一段文本",
    "timestamp": "14:31:10"
  }
]
```

### 6.2 设置数据格式

```json
{
  "output_mode": "comma",        // 输出模式: comma/sequential/reverse
  "theme": "light",              // 主题: light/dark
  "color_scheme": "pure_blue"    // 配色: blue_gradient/pure_blue/pure_green
}
```

---

## 7. 快捷键映射

| 快捷键 | 功能 | 实现位置 |
|--------|------|----------|
| Ctrl+C | 自动捕获剪贴板 | 系统级，应用监听 |
| Ctrl+Space | 批量粘贴 | `register_hotkeys()` |
| Ctrl+Shift+C | 显示/隐藏窗口 | `register_hotkeys()` |
| Ctrl+Shift+Q | 退出程序 | `register_hotkeys()` |

---

## 8. 信号与槽机制

```python
# 自定义信号定义 (ClipboardWindow:383-387)
clipboard_changed_signal = pyqtSignal()   # 剪贴板变化
toggle_window_signal = pyqtSignal()       # 切换窗口显示
quit_app_signal = pyqtSignal()            # 退出应用
do_paste_signal = pyqtSignal()            # 执行粘贴

# 信号连接 (ClipboardWindow:427-431)
clipboard_changed_signal.connect(on_clipboard_changed)
toggle_window_signal.connect(do_toggle_window)
quit_app_signal.connect(do_quit_app)
do_paste_signal.connect(do_paste)
```

**设计目的:** 确保跨线程操作（如全局快捷键回调）在主线程中安全执行

---

## 9. 防冲突机制

### 9.1 单实例检测

使用 Windows 互斥量 (Mutex) 确保只有一个程序实例运行:

```python
mutex_name = "Global\\ClipboardHelperMutex_UniqueID_20231124"
mutex = kernel32.CreateMutexW(None, False, mutex_name)
last_error = kernel32.GetLastError()

if last_error == 183:  # ERROR_ALREADY_EXISTS
    # 显示提示，阻止启动
```

### 9.2 粘贴内容去重

防止自己粘贴的内容被再次捕获:

```python
# 时间窗口检查 (main.py:468)
if current_text == self.last_pasted_text and \
   current_time < self.last_paste_time + 3.0:
    return  # 忽略

# 粘贴忽略时间窗口 (main.py:461)
if current_time < self.paste_ignore_until:
    return  # 忽略
```

---

## 10. 界面设计

### 10.1 主窗口布局

```
┌─────────────────────────────────────┐
│  ┌─────────────────────────────┐   │  ← 渐变头部 (50px)
│  │ 剪贴板 (5)  [拼][🗑][⚙][×]  │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ ┌─────────────────────────┐ │   │
│  │ │ 文本1         14:30  [×] │ │   │  ← 列表项 (40px)
│  │ └─────────────────────────┘ │   │
│  │ ┌─────────────────────────┐ │   │
│  │ │ 文本2         14:31  [×] │ │   │
│  │ └─────────────────────────┘ │   │
│  │ ...                         │   │
│  └─────────────────────────────┘   │  ← 列表区域（可滚动）
└─────────────────────────────────────┘
```

**尺寸:** 360x560 像素
**位置:** 屏幕右下角 (距边缘 20px)

### 10.2 设置对话框布局

```
┌─────────────────────────────────────┐
│  ┌─────────────────────────────┐   │  ← 渐变头部
│  │ 设置                    [×] │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ 输出模式                    │   │
│  │ 选择粘贴时的输出方式         │   │  ← 设置项
│  │                 [逗号拼接 ▼] │   │
│  │                             │   │
│  │ 系统主题                    │   │
│  │ 选择界面显示主题             │   │
│  │                 [日间模式 ▼] │   │
│  │                             │   │
│  │ 配色方案                    │   │
│  │ 选择界面的配色风格           │   │
│  │                 [纯蓝色  ▼] │   │
│  │                             │   │
│  │          [应用][确定][取消]  │   │  ← 按钮组
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

**尺寸:** 380x300 像素
**特性:** 无边框、可拖拽、半透明阴影

---

## 11. 性能优化

1. **剪贴板轮询间隔:** 300ms，平衡响应速度和 CPU 占用
2. **粘贴延迟机制:**
   - 批量粘贴: 250ms 延迟执行
   - 单项粘贴: 400ms 延迟（留出窗口切换时间）
3. **重试机制:** 剪贴板读写操作支持 3 次重试，50ms 间隔
4. **数据去重:** 添加剪贴板数据时自动去重

---

## 12. 安全性

1. **异常处理:** 所有关键操作均包含 try-except 异常捕获
2. **剪贴板锁定:** 使用 Windows 剪贴板锁机制，避免冲突
3. **单实例检测:** 防止多开导致数据冲突
4. **数据持久化:** 自动创建数据目录，确保文件路径有效

---

## 13. 扩展性

### 13.1 添加新主题

在 `ThemeManager.COLOR_SCHEMES` 中添加新配色:

```python
COLOR_SCHEMES = {
    'your_theme': {
        'light': ('#color1', '#color2'),
        'dark': ('#color3', '#color4')
    }
}
```

### 13.2 添加新快捷键

在 `register_hotkeys()` 中注册:

```python
self.hotkey_listener = GlobalHotKeys({
    '<ctrl>+<alt>+n': your_callback
})
```

### 13.3 添加新输出模式

1. 在设置中添加新选项
2. 在 `do_paste()` 中添加新模式逻辑

---

## 14. 依赖关系图

```
main.py
 ├─► themes.theme_manager
 │    └─► ThemeManager
 ├─► ui.components.clickable_label
 │    └─► ClickableLabel
 └─► utils
      ├─► clipboard_utils
      │    ├─► get_clipboard_text()
      │    ├─► set_clipboard_text()
      │    └─► process_clipboard_text()
      ├─► keyboard_simulator
      │    ├─► simulate_paste()
      │    └─► simulate_key_combo()
      ├─► window_manager
      │    ├─► get_foreground_window()
      │    ├─► set_foreground_window()
      │    └─► WindowFocusManager
      └─► data_manager
           └─► ClipboardDataManager
```

---

## 15. 潜在改进方向

1. **性能优化:**
   - 使用 Windows 剪贴板监听 API 替代轮询
   - 虚拟化长列表渲染

2. **功能增强:**
   - 支持富文本/图片剪贴板
   - 添加搜索过滤功能
   - 支持剪贴板分组管理
   - 云同步功能

3. **用户体验:**
   - 添加键盘导航支持
   - 支持自定义快捷键
   - 添加历史记录统计

4. **代码质量:**
   - 分离 UI 和业务逻辑（MVC/MVP 模式）
   - 添加单元测试
   - 使用配置文件管理常量

---

## 16. 开发指南

### 16.1 开发环境设置

```bash
# 安装依赖
pip install -r requirements.txt

# 运行程序
python main.py
```

### 16.2 打包发布

参考 `BUILD_GUIDE.txt` 使用 PyInstaller 打包

### 16.3 调试技巧

- 程序输出使用 `print()` 记录到控制台
- 关键异常会打印错误信息
- 系统托盘图标显示运行状态

---

## 附录

### A. 文件大小统计

| 文件 | 行数 | 主要内容 |
|------|------|----------|
| main.py | 1281 | 主程序逻辑 |
| theme_manager.py | 485 | 主题管理 |
| clipboard_utils.py | 87 | 剪贴板工具 |
| keyboard_simulator.py | 155 | 键盘模拟 |
| window_manager.py | 95 | 窗口管理 |
| data_manager.py | 154 | 数据管理 |
| clickable_label.py | 49 | UI 组件 |

**总计:** ~2306 行代码

### B. 关键代码位置索引

- 单实例检测: `main.py:1225-1253`
- 剪贴板监听: `main.py:456-476`
- 批量粘贴: `main.py:535-596`
- 单项粘贴: `main.py:1032-1062`
- 主题切换: `main.py:1183-1202`
- 快捷键注册: `main.py:511-533`
- 数据持久化: `main.py:1114-1159`

---

**文档版本:** 1.0
**最后更新:** 2025-11-25
**作者:** 系统架构分析
